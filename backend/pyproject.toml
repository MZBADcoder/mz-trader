[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "trader-refactor-backend"
version = "0.1.0"
description = "Backend scaffold for trader-refactor"
requires-python = ">=3.11,<4.0"
dependencies = [
  "fastapi",
  "uvicorn[standard]",
  "pydantic-settings",
  "sqlalchemy>=2",
  "greenlet",
  "asyncpg",
  "alembic",
  "redis",
  "httpx",
  "massive>=2.2,<3",
  "websockets",
]

[project.optional-dependencies]
dev = [
  "pytest",
  "httpx",
  "ruff",
  "testcontainers",
]

[tool.poetry]
package-mode = false

[tool.setuptools]
package-dir = {"" = "src"}
py-modules = ["main", "settings"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
